#include "main.h"

/**
 * _strlen - finds the longht of a string
 * @s: string to be counted
 * Return: lenght of a string
 */

int _strlen(char *s)
{
	int length = 0;

	while(length[s] != '\0')
	{
		length++;
	}
	return length;
}
